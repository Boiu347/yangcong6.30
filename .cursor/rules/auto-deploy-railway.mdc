---
description: 完成 yangcong-repo 改动后自动 commit 并 push 到 Railway
alwaysApply: true
---

# 自动部署到 Railway

在 `yangcong-repo`（InsightHub｜洞见中枢）完成用户请求的代码改动后：

1. 确认改动可运行、无敏感信息（`.env`、密钥等）
2. 自动 `git add` 相关文件并 `git commit`（简洁说明 why）
3. 自动 `git push origin main` 触发 Railway 部署
4. 在回复中告知用户已推送，部署通常需 1–3 分钟

无需用户再单独说「部署」「推送 Railway」。

例外：用户明确说「先别部署」「只改本地」时，不 push。
